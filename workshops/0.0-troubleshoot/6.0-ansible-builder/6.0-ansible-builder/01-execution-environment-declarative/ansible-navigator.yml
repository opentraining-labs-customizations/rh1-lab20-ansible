---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: aaphub001.lnx.example.local/vmware:v1
    pull:
      policy: always
    volume-mounts:
    - dest: /repository/
      src: /repository/
  logging:
    level: critical
  mode: stdout
